%tr
  %td= alert.name
  %td= alert.send_to.capitalize
  %td= alert.emails
  %td
    %table
      %tr
        %td= link_to 'Edit', edit_platform_alert_path(@platform, alert), class: "btn btn-primary"
        %td= link_to 'Delete', platform_alert_path(@platform, alert), confirm: 'Are you sure?', method: :delete, class: "btn btn-danger delete"